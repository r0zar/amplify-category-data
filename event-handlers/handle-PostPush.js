const eventName = "PostPush";

async function run(context, args) {
  // context.print.info(
  //   `Upload 🗑️: https://s3.console.aws.amazon.com/s3/buckets/${props.appId}-data-import-${props.env}`
  // );
  // console.log(
  //   context.amplify.getProjectMeta().providers.awscloudformation.Region
  // );
}

module.exports = {
  run,
};
